# setexpr fmt converts the hex cell values that "fdt get value" renders
# into the decimal pin numbers the gpio command expects (the boot-pin
# convention block in bootenv.cmd.in).
CONFIG_CMD_SETEXPR_FMT=y
